package sri.examples.lists.enumfeatuers;

//enums are special data types that contains predefined constants.
// enums is little like an array, except its elements are known, not changeable, and each element
// can be referred to by a constant name, instead of an index position.

// an enum, in its simplest form, is described like a class, but keyword enum, replaces the keyword class.
public enum DayOfTheWeek {
    SUN,MON,TUES,WED,THURS,FRI,SAT
}
